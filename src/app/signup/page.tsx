import SignUp from "@/components/sections/SignUp";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign Up | Lunoa",
};

export default function VerifyBusinessPartnerPage() {
  return <SignUp />;
}
